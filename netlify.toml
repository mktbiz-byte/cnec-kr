[build]
  command = "npm install && npm run build"
  publish = "dist"
  environment = { NODE_VERSION = "20.19.0" }

[dev]
  command = "npm run dev"
  port = 3000

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[functions]
  directory = "netlify/functions"
